version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: elix-star-live:latest
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production
      PORT: "8080"
    restart: unless-stopped
