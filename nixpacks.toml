# Use npm install so the build succeeds when package-lock.json is out of sync
# (e.g. different branch or cache). For reproducible builds, keep package-lock.json
# committed and in sync with package.json, then you can remove this override.
[phases.install]
cmds = ["npm install"]
